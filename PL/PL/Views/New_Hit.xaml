<Window x:Class="PL.Views.New_Hit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Views"
        xmlns:local1="clr-namespace:PL.ViewModel"
        mc:Ignorable="d"
        Title="New_Hit" Height="300" Width="300">
    <Window.Resources>
        <local1:ReportConverter x:Key="reportConverter"></local1:ReportConverter>
    </Window.Resources>
    <Grid>
        <Label FontSize="20" Content="please fill the next fields" HorizontalAlignment="Center" Margin="0,10" VerticalAlignment="Top"/>
        <Image x:Name="image" HorizontalAlignment="Left" Height="100" Margin="95,124,0,0" VerticalAlignment="Top" Width="100"/>
        <TextBox x:Name="location" HorizontalAlignment="Left" Height="23" Margin="85,85,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <TextBox x:Name="time" HorizontalAlignment="Left" Height="23" Margin="85,52,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>

        <Button Command="{Binding AddHitCommand}" Content="submit" HorizontalAlignment="Left" Margin="107,239,0,0" Click="Submit_Click" VerticalAlignment="Top" Width="75">
            <Button.CommandParameter>
                <MultiBinding Converter="{StaticResource reportConverter}">
                    <Binding Path="Image" ElementName="image"/>
                    <Binding Path="Text" ElementName="time"/>
                    <Binding Path="Text" ElementName="location"/>
                </MultiBinding>
            </Button.CommandParameter>
        </Button>

    </Grid>
</Window>
