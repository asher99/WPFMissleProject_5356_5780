<Window x:Class="PL.Views.New_Report"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:toolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input.Toolkit"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:local1="clr-namespace:PL.Converters"
        mc:Ignorable="d"
        Title="New_Report" Height="300" Width="300">
    
    <Window.Resources>
        <local1:ReportConverter x:Key="reportConverter"></local1:ReportConverter>
    </Window.Resources>
    <Grid>
        <Label FontSize="20" Content="Please fill the next fields" HorizontalAlignment="Center" Margin="0,10" VerticalAlignment="Top"/>
        <TextBox x:Name="Name" HorizontalAlignment="Center" Height="23" Margin="80,60,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <TextBox x:Name="Time" HorizontalAlignment="Center" Height="23" Margin="80,100,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <toolkit:AutoCompleteBox x:Name="Location" TextChanged="ReportLocation_TextChanged" ItemsSource="{Binding locations}"  Margin="126,165,46,46" />
        <TextBox x:Name="NumOfFalls" HorizontalAlignment="Center" Height="23" Margin="126,137,46,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Button Command="{Binding addReportCommand}" Content="submit" HorizontalAlignment="Center" Margin="108,220,108,0"  VerticalAlignment="Top" Click="Submit_Click" Width="76" Height="24">
            <Button.CommandParameter>
                <MultiBinding Converter="{StaticResource reportConverter}">
                    <Binding Path="Text" ElementName="Name"/>
                    <Binding Path="Text" ElementName="Time"/>
                    <Binding Path="Selectedlocation" ElementName="Location"/>
                    <Binding Path="Text" ElementName="NumOfFalls"/>
                </MultiBinding>
            </Button.CommandParameter>
        </Button>
        <Label Content="name:" HorizontalAlignment="Left" Margin="51,57,0,0" VerticalAlignment="Top"/>
        <Label Content="Time:" HorizontalAlignment="Left" Margin="51,97,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.414,0.263"/>
        <Label Content="Location:" HorizontalAlignment="Left" Margin="43,177,0,0" VerticalAlignment="Top"/>
        <Label Content="NumOfFalls:" HorizontalAlignment="Left" Margin="36,137,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
